{
  "name": "ai-tutor-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Command-line client for the ai-tutor to-do API",
  "type": "module",
  "bin": {
    "ai-tutor": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "node build.mjs",
    "prepare": "node build.mjs"
  },
  "dependencies": {
    "better-auth": "^1.7.4",
    "commander": "^15.0.0",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "ai-tutor-todo-api": "*",
    "esbuild": "^0.28.2"
  }
}
